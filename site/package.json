{
  "name": "punk-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:pages": "wrangler pages deploy dist --project-name=punk --branch=main",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.8"
  }
}
