id: eval_contract_scope_required_for_cut
phase: 2
status: draft
scope_status: active-core
law_refs: ["5", "8", "18", "19"]
flow_refs: ["run_contract"]
research_refs:
  - knowledge/research/2026-04-19-eval-plane-prior-art.md
decision_refs:
  - docs/adr/ADR-0008-eval-plane.md
fixture_refs:
  - fixtures/contracts/approved_without_scope.yaml
command_under_test:
  cli: "punk cut run --contract fixtures/contracts/approved_without_scope.yaml"
pre_state:
  flow_state: approved
  contract:
    status: approved
    allowed_scope: []
expected_result:
  exit_code: 1
  denied: true
  reason_contains: "scope"
expected_events:
  []
expected_artifacts:
  required: []
  forbidden:
    - run_ref
    - receipt_ref
hard_gate: true
scorecard_metrics:
  - scope_guard_checked
  - forbidden_artifacts_absent
waiver_policy:
  allowed: false
report_output:
  include_in: [machine_json, human_markdown]
notes:
  - Checks that an approved contract still cannot run without non-empty allowed scope.
