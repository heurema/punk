# Duplicate definition candidate cases

Fixture-style cases for the advisory duplicate-definition candidate check on touched canonical docs.

Scope of this slice:

- warning-level only;
- touched/new canonical docs only;
- shared term definitions remain glossary-only in v0;
- expected results are `pass`, with warnings only for definition-like H2 sections outside `docs/product/GLOSSARY.md`.

Check family name used in these fixtures:

- `docs_duplicate_definition_candidate`
