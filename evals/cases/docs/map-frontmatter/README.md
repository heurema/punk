# Map/frontmatter consistency cases

Fixture-style cases for the advisory consistency check between `docs/product/DOCUMENTATION-MAP.md` and canonical doc frontmatter.

Scope of this slice:

- warning-level only;
- `DOCUMENTATION-MAP.md` stays a reader-facing view;
- canonical doc frontmatter stays the owner authority surface;
- expected results are `pass` with warnings where drift exists.

Check family name used in these fixtures:

- `docs_map_frontmatter_view_consistency`
